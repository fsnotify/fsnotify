# Watch a symlink to a file.
require symlink

touch /file
ln -s /file /link
watch /link

echo hello >>/file

Output:
	write    /link

	kqueue:   # TODO: Should use the link name.
		write    /file
	windows:  # TODO: see if we can fix this.
		no-events
